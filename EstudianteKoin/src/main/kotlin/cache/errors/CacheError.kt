package org.example.cache.errors

/**
 * Clase que representa un error relacionado con la caché.
 * @property message El mensaje descriptivo del error.
 * @constructor Crea un nuevo objeto [CacheError] con el mensaje de error especificado.
 */
class CacheError(val message: String)